# إعداد البيئة بتاعتك!

<div dir="rtl">

## نظرة عامة

هنستخدم `Conda` كمدير الحزم بتاعنا. لو عندك تفضيل لـ `venv` أو `poetry`، اتفضل استخدمهم براحتك.

Conda هو مدير حزم بيساعد في إدارة بيئات برامج مختلفة. لو بتتابع على Codespaces، يبقى مثبت عندك فعلاً! لو بتتابع محلياً وماكنتش نصبت `Conda` بعد، روح لموقع [Anaconda الرسمي](https://www.anaconda.com/products/individual) عشان تشوف التعليمات إزاي تحمل وتنصب Conda.

## إنشاء بيئة Conda جديدة

افتح الـ terminal أو command prompt واجري الأمر ده عشان تنشئ بيئة Conda جديدة اسمها `lil_llama_index`، وتنصب Python 3.10.

```bash
conda create --name lil_llama_index python==3.10 python-dotenv
```

## تفعيل بيئة Conda

بمجرد ما البيئة تتنشئ، فعلها باستخدام الأمر ده:

```bash
conda activate lil_llama_index
```

## تنصيب Jupyter و JupyterLab

Jupyter هي منصة حوسبة تفاعلية مشهورة، و JupyterLab هي واجهة المستخدم الجيل القادم بتاعتها. تقدر تنصبهم الاتنين باستخدام الأمر ده:

```bash
conda install ipykernel jupyter jupyterlab
```

## التحقق من التنصيب

ده هيفتح JupyterLab في المتصفح الافتراضي بتاعك، ويسمح لك تنشئ وتشغل دفاتر Jupyter.

```bash
jupyter lab
```

## تنصيب 🗂️ LlamaIndex 🦙

LlamaIndex مفصول بشكل جميل لحزم أصغر كتيرة.

دلوقتي، خلينا ننصب حزمة البداية بس. مع تقدمنا في الكورس هضيف مكتبات إضافية حسب الحاجة مع `pip install ...` في الخلية الأولى من كل notebook.

```bash
pip install llama-index==0.10.37
```

**ملاحظة مهمة:** LlamaIndex مكتبة جديدة، وبتتغير بسرعة. من المهم إنك تثبت الإصدار بتاعك على اللي فوق عشان ماتقعش في أي أخطاء كود وأنت بتتابع معايا خلال الكورس.

## تنصيب Qdrant

هنستخدم Qdrant كقاعدة بيانات المتجهات بتاعتنا خلال الكورس ده. خلينا ننصب المتطلبات بتاعتها.

```bash
pip install qdrant-client==1.9.1
```

تاني، من المهم تثبيت الإصدار على اللي فوق عشان نكون على نفس الصفحة وإحنا بنكتب كود.

## تنصيب متطلبات LlamaIndex لـ Qdrant

LlamaIndex متطلباتها مفصولة بشكل جميل. خلينا ننصب اللي محتاجينه لـ Qdrant:

```bash
pip install llama-index-vector-stores-qdrant==0.2.8 llama-index-readers-file==0.1.22
```

## تنصيب مكتبات LLM

هنستخدم Cohere و OpenAI بشكل متكرر خلال الكورس، فخلينا ننصب المتطلبات بتاعتهم كمان.

```bash
pip install cohere==5.5.0 
pip install openai==1.30.1
pip install llama-index-llms-cohere==0.2.0 
pip install llama-index-llms-openai==0.1.19
pip install llama-index-embeddings-cohere==0.1.8
pip install llama-index-embeddings-openai==0.1.9
pip install llama-index-postprocessor-cohere-rerank==0.1.6 
```

## ربط IPython kernel ببيئة Conda

```bash
python -m ipykernel install --user --name=lil_llama_index --display-name "LlamaIndex (LinkedIn Learning)"
```

## أتمتة الإعداد

ملحوظة: عشان أسهل عليك لو اضطريت تنهي الـ codespace بتاعك وتبدأ من الصفر، أنا عملت bash script تقدر تشغله. موجود في المجلد ده واسمه `install.sh`.

### عشان تشغل السكريبت ده:

- اعمل السكريبت قابل للتنفيذ: `chmod +x install.sh`
- شغل السكريبت: `./install.sh`

</div>
