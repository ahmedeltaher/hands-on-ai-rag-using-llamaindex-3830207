# استخدام نماذج اللغة الكبيرة (LLMs)

<div dir="rtl">

## اختيار النموذج

لما بتبني تطبيق مبني على LLM، واحد من أول القرارات اللي بتاخدها هو إنك تختار أي نموذج (أو نماذج) هتستخدم (طبعاً، تقدر تستخدم أكتر من واحد لو عايز).

## استخدام LLM في خط الأنابيب

الـ LLM هيتستخدم في مراحل مختلفة من خط الأنابيب بتاعك، بما في ذلك:

### خلال الفهرسة:

- 👩🏽‍⚖️ للحكم على ملاءمة البيانات (هل نفهرس ولا لا).
- 📖 تلخيص البيانات وفهرسة الملخصات دي.

### خلال الاستعلام:

- 🔎 **الاسترجاع**: جلب البيانات من الفهرس بتاعك، اختيار أفضل مصدر بيانات من الخيارات، حتى استخدام أدوات لجلب البيانات.

- 💡 **تركيب الاستجابة**: تحويل البيانات المستَرجعة لإجابة، دمج الإجابات، أو تحويل البيانات (زي من نص لـ JSON).

## واجهة موحدة

LlamaIndex بيديك واجهة موحدة لنماذج لغة كبيرة مختلفة. ده معناه إنك تقدر بسهولة تمرر أي LLM تختاره في أي مرحلة من خط الأنابيب.

في الكورس ده هنستخدم بشكل أساسي OpenAI. تقدر تشوف قائمة كاملة بتكاملات LLM [هنا](https://docs.llamaindex.ai/en/stable/module_guides/models/llms/modules.html) وتستخدم مزود LLM اللي تختاره.

## طرق استخدام LLM

### الإكمال البسيط (Complete)

استخدام LLM لإكمال نص أو prompt بسيط.

### استخدام القوالب (Templates)

استخدام `PromptTemplate` لإنشاء prompts ديناميكية بمتغيرات قابلة للتخصيص.

### المحادثة (Chat)

استخدام رسائل محادثة مع أدوار مختلفة (system, user, assistant) للتفاعل مع LLM.

### قوالب المحادثة (Chat Templates)

إنشاء قوالب محادثة مع رسائل نظام ومستخدم قابلة للتخصيص.

### البث المباشر (Streaming)

استخدام `stream_chat` لعرض الاستجابة بشكل تدريجي، مفيد لتحسين تجربة المستخدم.

### محرك المحادثة (Chat Engine)

استخدام `SimpleChatEngine` لإنشاء جلسة محادثة تفاعلية مع LLM.

</div>
