# تحميل البيانات

<div dir="rtl">

## 📂 تحميل البيانات

تجهيز بياناتك لـ LLM بيتضمن خط أنابيب إدخال شبيه بتنظيف بيانات ML أو عمليات ETL التقليدية.

## مراحل خط أنابيب الإدخال

- 📥 تحميل البيانات
- 🔧 تحويل البيانات
- 🗃️ فهرسة وتخزين البيانات

## تحميل ملفات تجريبية

أول حاجة، خلينا نحمل بعض الملفات التجريبية من Project Gutenberg.

## 📥 تحميل البيانات

لاستخدام البيانات مع LLM، أول حاجة لازم تحملها باستخدام موصلات البيانات، المعروفة بـ `Readers` في LlamaIndex، واللي بتنسق البيانات في كائنات `Document` تحتوي على بيانات وبيانات وصفية.

### 📚 SimpleDirectoryReader

- المحمل الأكثر مباشرة هو `SimpleDirectoryReader`.
- مدمج في LlamaIndex، بيقرأ تنسيقات مختلفة (Markdown، PDFs، مستندات Word، عروض PowerPoint، صور، صوت، فيديو) من كل ملف في مجلد، بينشئ مستندات.

## إنشاء كائنات Document يدوياً

تقدر تنشئ كائنات `Document` يدوياً كمان.

### إضافة البيانات الوصفية

تقدر تضيف بيانات وصفية في constructor المستند أو بعد إنشاء المستند.

**طريقتين للإضافة:**
1. في constructor المستند مباشرة
2. بعد إنشاء المستند عن طريق تعديل خاصية `metadata`

## 🔧 تحويل البيانات

بعد التحميل، لازم نعالج ونحول البيانات للاسترجاع. لازم نحول قائمة كائنات `Document` لكائنات `Node`.

### مميزات التحويل:

- ✂️ التحويلات بتشمل التقطيع، استخراج البيانات الوصفية، وتضمين كل قطعة.

- 🌟 الـ Nodes هي مواطن من الدرجة الأولى في LlamaIndex، بتسمح بالتعريف المباشر أو التحليل من Documents.

- 🔄 مدخلات ومخرجات التحويل هي كائنات `Node` (ملاحظة: `Document` هو فئة فرعية من `Node`).

- 🛠️ الـ Nodes هي "قطع" من Documents، بتشمل نص، صور، إلخ، بالإضافة للبيانات الوصفية والعلاقات.

### NodeParser

- 📊 فئات `NodeParser` بتحول Documents لـ Nodes مع كل السمات الضرورية. في [عدد من](https://docs.llamaindex.ai/en/stable/module_guides/loading/node_parsers/modules.html) `NodeParser`'s تقدر تختار منها!

- 📑 **الـ API عالي المستوى**: استخدم `.from_documents()` للتحليل والتقطيع التلقائي لكائنات Document.

- 🔍 **العملية الأساسية**: تقسيم Document لكائنات Node، مع الحفاظ على النص والبيانات الوصفية مع رابط للـ Document الأصلي.

## بناء Nodes يدوياً

تقدر كمان تختار إنشاء كائنات Node يدوياً.

## NodeRelationships (علاقات العقد)

تقدر تعين علاقات بين العقد.

### فوائد NodeRelationships:

- 🌐 بتعين اتصالات بين قطع النص. مفيدة لـ:
  - المستندات المنظمة بشكل هرمي (مثل كتاب، فصل، قسم، قسم فرعي)
  - الحفاظ على الترتيب التسلسلي
  - علاقات معقدة تانية (مثل في المستندات القانونية للروابط بند أو حالات تانية)

- 🔍 NodeRelationships بتساعد على استرجاع مش بس القسم ذي الصلة، لكن كمان الأقسام ذات الصلة اللي ممكن توفر سياق أو معلومات إضافية.

## تنظيف

في النهاية، ممكن نحذف ملفات النص اللي حملناها لأننا مش هنحتاجها في المستقبل.

</div>
