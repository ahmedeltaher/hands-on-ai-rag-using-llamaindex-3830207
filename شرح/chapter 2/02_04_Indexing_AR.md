# الفهرسة

<div dir="rtl">

## 🗂️ الفهرسة

`Index` هي هيكل بيانات بيسمح بالاسترجاع السريع للسياق ذي الصلة لاستعلام المستخدم.

هي الأساس الجوهري لحالات استخدام التوليد المعزز بالاسترجاع (RAG). الفهارس بتتبنى من `Documents` وبتُستخدم لبناء المستَرجعات، ومحركات الاستعلام، ومحركات المحادثة. كلهم بيمكنوا السؤال والجواب والمحادثة فوق بياناتك.

## نظرة عامة على العملية

- 📂 بعد تحميل بياناتك، تكون جاهز لبناء `Index`.

- 🌐 **Vector Store Index:** أكثر نوع Index شائع. بيقسم `Documents` بتاعك لـ `Nodes` وبيولد تضمينات متجهة لنص كل عقدة، بيجهزهم لاستعلامات LLM.

- 🔄 **عملية Vector Store Index:** تحليل النصوص الخام لكائنات مستند، تقسيم كائنات المستند لقطع/عقد، بعدين تحويل كل العقد لتضمينات وتخزينها في قاعدة بيانات متجهات.

## ⚙️ تضمين النص

أول حاجة، خلينا نشوف إيه هو التضمين (embedding).

### نماذج التضمين المتاحة

تقدر تستخدم نماذج تضمين مختلفة زي:
- Cohere (embed-english-v3.0, embed-english-light-v3.0, embed-english-v2.0)
- نماذج محلية من Hugging Face
- FastEmbed للأجهزة اللي شغالة على CPU

### استخدام نماذج Hugging Face المحلية

تقدر تستخدم نموذج تضمين من Hugging Face. اتفرج على [MTEB Leaderboard](huggingface.co/spaces/mteb/leaderboard) لمعرفة الأحدث.

### استخدام FastEmbed

لو شغال محلياً وعلى CPU، ممكن تحب تستخدم `FastEmbed`. النماذج دي خفيفة، مكممة، ومحسّنة للـ CPU. هنا [النماذج المدعومة](https://qdrant.github.io/fastembed/examples/Supported_Models/).

## إنشاء Index

### الخطوات:

1. تحميل البيانات من URL
2. إنشاء كائنات Document
3. بناء VectorStoreIndex

### ⏳ ملاحظة مهمة

توليد التضمينات ممكن يكون مستهلك للوقت، خصوصاً مع كميات كبيرة من النص، بسبب استدعاءات API الكتيرة المطلوبة.

## طرق إنشاء VectorStoreIndex

الـ `VectorStoreIndex` في LlamaIndex ممكن ينشأ بطريقتين:

### `from_documents`

لما يكون عندك مجموعة من المستندات عايز تفهرسها. الطريقة دي بتاخد المستندات دي، بتحسب تضمينها، وبتخزنها في مخزن المتجهات.

### `from_vector_store`

لما يكون عندك تضمينات محسوبة فعلاً ومخزنة في مخزن متجهات خارجي (زي Qdrant). الطريقة دي بتتصل بمخزن المتجهات الخارجي وبتستخدم التضمينات المحسوبة مسبقاً للفهرس.

## بناء Index من Nodes

ملاحظة: تقدر كمان تبني index فوق **قائمة من كائنات `Node`**.

خلينا نبني على النمط ده في الدرس اللي جاي، حيث هنخزن ونحفظ الفهرس بتاعنا للاستخدام المستقبلي.

</div>
