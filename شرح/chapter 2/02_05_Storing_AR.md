# التخزين

<div dir="rtl">

## 🗄️ التخزين

تحميل وفهرسة البيانات بيكلف وقت وفلوس.

افتراضياً، البيانات المفهرسة بتتخزن في الذاكرة. بس، تقدر تخزن بياناتك عشان تتجنب الوقت والتكاليف المرتبطة بإعادة فهرستها. أبسط طريقة لعمل ده هي **الحفظ على القرص**.

كل كائن `Index` عنده دالة `.persist()`، واللي هتكتب كل البيانات على القرص في المكان المحدد.

## الخطوات الأساسية

دلوقتي بعد ما حملنا البيانات، خلينا:

1. نحمل كـ Document
2. نحلل كـ Nodes
3. ننشئ index

## ☁️ استخدام قاعدة بيانات المتجهات

هنستخدم qdrant كقاعدة بيانات المتجهات المفضلة بتاعتنا خلال الكورس ده.

### لاستخدام qdrant لتخزين التضمينات من `VectorStoreIndex`، محتاج:

- تهيئة عميل qdrant
- إنشاء `Collection` لتخزين بياناتك في qdrant
- تعيين qdrant كـ `vector_store` في `StorageContext`
- تهيئة `VectorStoreIndex` بتاعك باستخدام `StorageContext` ده

## 🗃️ Storage Context (سياق التخزين)

`StorageContext` في `LlamaIndex` هو تجريد أساسي بيتمحور حوالين تخزين `Nodes`، الفهارس، والمتجهات. بيسهل تخزين واسترجاع البيانات.

### هو حاوية أدوات بتدعم التالي:

- `docstore`: [`BaseDocumentStore`](https://github.com/run-llama/llama_index/blob/main/llama-index-core/llama_index/core/storage/docstore/types.py) لتخزين العقد.

- `index_store`: [`BaseIndexStore`](https://github.com/run-llama/llama_index/blob/main/llama-index-core/llama_index/core/storage/index_store/types.py#L13) لتخزين الفهارس.

- `vector_store`: [`VectorStore`](https://github.com/run-llama/llama_index/blob/main/llama-index-core/llama_index/core/vector_stores/simple.py) لتخزين المتجهات.

- `graph_store`: [`GraphStore`](https://github.com/run-llama/llama_index/blob/main/llama-index-core/llama_index/core/graph_stores/simple.py) لتخزين الرسوم البيانية للمعرفة.

## 🪃 الاسترجاع (Retrieval)

`Retriever` هي واجهة مكشوفة بواسطة `Index`. `Index` مع `Retriever` بتاعه بيُستخدم لتخزين وجلب البيانات. الـ `Retriever` جزء من `Index` وبيُستخدم لاسترجاع البيانات المخزنة في الفهرس.

## أنواع المستَرجعات

LlamaIndex بيوفر [أنواع كتيرة مختلفة من المستَرجعات](https://github.com/run-llama/llama_index/tree/main/llama-index-core/llama_index/core/retrievers) لجلب المعلومات ذات الصلة من البيانات المدخلة بناءً على استعلام معين.

### أمثلة تشمل:

#### Vector Retriever (المستَرجع المتجه)

المستَرجع المتجه بيستخدم البحث بالتشابه المتجه لإيجاد أكثر العقد ذات صلة (قطع النص) بناءً على تضمين الاستعلام. بيحتاج قاعدة بيانات متجهات للتخزين والبحث عبر تضمينات العقد.

#### Fusion Retriever (المستَرجع الدمجي)

المستَرجع الدمجي بيولد استعلامات متعددة من الاستعلام الأصلي، بيؤدي استرجاع على مجموعة من المستَرجعات لكل استعلام، وبعدين بيدمج ويعيد ترتيب النتائج عبر كل الاستعلامات. ده بيهدف لالتقاط نية الاستعلام بشكل أفضل من خلال إعادة كتابة الاستعلام والتجميع.

#### Recursive Retriever (المستَرجع التكراري)

المستَرجع التكراري بيسمح بالاسترجاع الهرمي عن طريق استرجاع العقد الخشنة الأول وبعدين استرجاع العقد الأدق تكرارياً ضمن العقد الخشنة دي. ده ممكن يكون مفيد للفهرسة والاسترجاع متعدد المستويات.

تقدر كمان تدمج المستَرجعات بطرق مثيرة وتبني استراتيجيات استرجاع أكثر تقدماً، زي ما هنشوف لاحقاً في الكورس ده.

## في المثال هنا، بنستخدم Vector Retriever

- 🔍 لما بتبحث، استعلامك بيتحول كمان لتضمين متجه.

- 🗂️ الـ `VectorStoreIndex` بعدين بيؤدي عملية رياضية لترتيب التضمينات بناءً على التشابه الدلالي لاستعلامك.

- 🔝 استرجاع top-k الدلالي هو أبسط طريقة للاستعلام عن فهرس متجه.

- ⩬ تقدر كمان تطبق عتبة تشابه (مثلاً، ترجع النتائج اللي أكثر تشابهاً من قيمة معينة بس).

## ملاحظة

بس، الفرص إنك مش بس عايز المستندات المرجوعة. إنت عايز المستندات تتركب في استجابة.

خلينا نبني على النمط ده في الدرس اللي جاي ونشوف إزاي نقدر نجيب استجابة بناءً على المستندات المستَرجعة دي.

## إغلاق العميل

في النهاية، لازم تقفل العميل عشان ما تتقفلش من الفهرس.

</div>
