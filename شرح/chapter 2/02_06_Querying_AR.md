# الاستعلام

<div dir="rtl">

## نظرة عامة

- 📊 دلوقتي بعد ما حملت بياناتك وبنيت فهرس، وقت التركيز على جوهر تطبيق LLM: الاستعلام.

- 🤖 الاستعلام في أبسط صوره بيتضمن عمل استدعاء prompt لـ LLM - ده ممكن يكون سؤال، طلب ملخص، أو إعطاء تعليمات أكثر تعقيداً.

- 🔗 للاستخدامات الأكثر تقدماً، الاستعلام ممكن يشمل استدعاءات prompt متكررة أو متسلسلة لـ LLM، أو حتى حلقة استدلال عبر مكونات متعددة.

## 🧐 محرك الاستعلام (QueryEngine)

محرك الاستعلام هو بنية أعلى مستوى بتستخدم `Index` (وبالامتداد، `Retriever`) للإجابة على الاستعلامات.

مش بس بيسترجع البيانات ذات الصلة لكن كمان بيعالجها لتوليد استجابة للاستعلام. `Query Engine` بيستخدم `Retriever` لجلب البيانات وبعدين بيطبق منطق إضافي لتوليد استجابة.

## إيه اللي بيحصل تحت الغطاء:

### 📚 الاسترجاع (Retrieval)

إيجاد وإرجاع أكثر المستندات ذات صلة من `Index` باستخدام استراتيجيات زي استرجاع "top-k" الدلالي.

### 🔧 المعالجة اللاحقة (Postprocessing)

اختيارياً إعادة ترتيب، تحويل، أو تصفية العقد المستَرجعة، غالباً بناءً على بيانات وصفية محددة زي الكلمات المفتاحية.

### 🔄 تركيب الاستجابة (Response Synthesis)

دمج الاستعلام، البيانات ذات الصلة، والprompt لتوليد استجابة من LLM بتاعك.

## ملاحظة مهمة

في [مجموعة واسعة من محركات الاستعلام](https://github.com/run-llama/llama_index/tree/main/llama-index-core/llama_index/core/query_engine) متاحة في LlamaIndex. مش هنتطرق لكلهم في الكورس ده، بس بشجعك تستكشف إيه المتاح وتفكر في إزاي ممكن تستخدمهم.

## الاستعلام البسيط

استخدام محرك الاستعلام للحصول على استجابة من الفهرس.

### البث المباشر (Streaming)

تقدر تستخدم streaming لعرض الاستجابة بشكل تدريجي.

### الوصول للعقد المصدر

بعد الاستعلام، تقدر توصل للعقد المصدر اللي استُرجعت واستُخدمت في توليد الاستجابة.

## 💬 محرك المحادثة (Chat Engine)

تقدر تحول الفهرس لمحرك محادثة للتفاعل بشكل أكثر طبيعية.

## أوضاع المحادثة

### Simple (بسيط)

محادثة مع LLM، بدون استخدام قاعدة معرفة. لاستخدام الوضع ده اعمل set `chat_mode="simple"`.

يوافق [`SimpleChatEngine`](https://github.com/run-llama/llama_index/blob/main/llama-index-core/llama_index/core/chat_engine/simple.py).

### Condense Question (تكثيف السؤال)

توليد سؤال مستقل من سياق المحادثة والرسالة الأخيرة. بعدين، اسأل محرك الاستعلام عن استجابة. لاستخدام الوضع ده اعمل set `chat_mode="condense_question"`.

يوافق [`CondenseQuestionChatEngine`](https://github.com/run-llama/llama_index/blob/main/llama-index-core/llama_index/core/chat_engine/condense_question.py).

### Context (سياق)

استرجاع نص من الفهرس بناءً على رسالة المستخدم. استخدم السياق ده لصياغة استجابة. لاستخدام الوضع ده اعمل set `chat_mode="context"`.

يوافق [`ContextChatEngine`](https://github.com/run-llama/llama_index/blob/main/llama-index-core/llama_index/core/chat_engine/context.py).

### Condense Plus Context (تكثيف + سياق)

تكثيف محادثة وآخر رسالة مستخدم لسؤال مستقل. بعدين بناء سياق للسؤال المستقل من مستَرجع. أخيراً، تمرير السياق مع prompt ورسالة المستخدم لـ LLM لتوليد استجابة. لاستخدام الوضع ده اعمل set `chat_mode="condense_plus_context"`.

يوافق [`CondensePlusContextChatEngine`](https://github.com/run-llama/llama_index/blob/main/llama-index-core/llama_index/core/chat_engine/condense_plus_context.py).

### ReACT

يوافق [`ReActAgent`](https://github.com/run-llama/llama_index/blob/37c95965426bddae82cec1ad49d3aa82e8bfe819/llama-index-core/llama_index/core/agent/react/base.py#L36).

استخدام حلقة وكيل ReAct مع أدوات محرك استعلام. لاستخدام الوضع ده اعمل set `chat_mode="react"`.

### Best (أفضل)

اختيار أفضل محرك محادثة بناءً على LLM الحالي. لاستخدام الوضع ده اعمل set `chat_mode="best"`.

يوافق `OpenAIAgent` لو بتستخدم نموذج OpenAI بيدعم واجهة برمجة استدعاء الدوال، وإلا، يوافق `ReActAgent`.

## تخصيص الاستعلام

- 🔧 **تخصيص الاسترجاع**: استخدام واجهة برمجة التكوين منخفضة المستوى في LlamaIndex لضبط قيمة `top_k` للحصول على تحكم أدق في نتائج الاستعلام.

- 📈 **إضافة معالجة لاحقة**: تنفيذ خطوة لضمان تضمين العقد اللي بتحقق حد أدنى من درجة التشابه بس، موازنة بين ثراء البيانات والصلة.

- 🎚️ **SimilarityPostprocessor**: تعيين عتبة درجة تشابه، متوافقة فقط مع المستَرجعات المبنية على التضمين، لضمان صلة عالية.

## إغلاق العميل

في النهاية، لازم تقفل العميل.

</div>
