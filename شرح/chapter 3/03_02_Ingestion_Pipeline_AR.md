# خط أنابيب الإدخال

<div dir="rtl">

## نظرة عامة على IngestionPipeline

- 🔄 **نظرة عامة على IngestionPipeline**: بيستخدم `Transformations` مطبقة على بيانات المدخلات، بتعدل البيانات لعقد (nodes)، واللي بترجع أو تتحط في قاعدة بيانات المتجهات.

- 💾 **آلية التخزين المؤقت**: كل زوج عقدة+تحويل بيتخزن مؤقتاً، بيحسن الكفاءة للعمليات المتطابقة اللاحقة عن طريق استخدام النتائج المخزنة مؤقتاً.

## استخدام `IngestionPipeline`

أول حاجة، خلينا نقرأ شوية بيانات.

## خط أنابيب الإدخال مع إدارة المستندات

### مميزات التخزين المؤقت وإدارة المستندات

- 💾 **التخزين المؤقت في IngestionPipeline**: بيعمل hash ويخزن كل مزيج عقدة + تحويل لتسريع العمليات المستقبلية بنفس البيانات.

- 📁 **إدارة التخزين المؤقت المحلي**: قائمة العقد المدخلة وزوج التحويل بيتخزنوا مؤقتاً في خط الأنابيب. لما نطبق نفس التحويل على نفس قائمة العقد تاني، العقد الناتجة بتتسترجع من التخزين المؤقت.

- 📚 **إرفاق Docstore**: بيمكن إدارة المستندات في خط أنابيب الإدخال، باستخدام `doc_id` أو `node.ref_doc_id` للتعريف. بيمنع تشغيل تحويل على نفس المستند أكتر من مرة عن طريق استخدام معرف المستند وhash محتوى المستند لإدارة التكرارات.

- 🗂️ **التعامل مع التكرارات**:
  - بيحافظ على خريطة `doc_id` لـ `document_hash` لتحديد التكرارات.
  - بيعيد معالجة المستندات لو نفس `doc_id` اتلقى بhash متغير.
  - بيتخطى المستندات لو نفس `doc_id` اتلقى بس الhash فضل زي ما هو.

- 🚫 **بدون Vector Store**:
  - محدود للتحقق وإزالة المدخلات المكررة.

- ✨ **مع Vector Store**:
  - بيمكن التعامل مع upserts للمستندات المحدثة، بيوفر قدرات إدارة متقدمة.

## حفظ التخزين المؤقت والـ Docstore

خط أنابيب الإدخال بيسمح بحفظ التخزين المؤقت والdocstore في مجلد افتراضي `(./pipeline_storage)`.

لما تشغل خط الأنابيب، بيعيد استخدام التخزين المؤقت، ويتخطى المستندات المكررة في الdocstore.

</div>
