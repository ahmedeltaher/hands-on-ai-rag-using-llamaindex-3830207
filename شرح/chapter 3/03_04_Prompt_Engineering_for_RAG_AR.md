# هندسة الـ Prompt لـ RAG

<div dir="rtl">

## ملاحظة عن التنصيبات

لازم تنصب الحزم دي على البيئة بتاعتك:
- `pip install datasets`
- `pip install llama-index-embeddings-fastembed`
- `pip install llama-index-llms-mistralai`

## قوالب Prompt الافتراضية

عشان تفهم إزاي النظام بيشتغل، لازم الأول تشوف قوالب الprompt الافتراضية اللي بيستخدمها النظام.

## تخصيص Prompt

ممكن تعمل prompt مخصص بتاعك عشان تتحكم في إزاي النظام بيرد على الاستعلامات.

**مثال على prompt مخصص:**

ده مثال على prompt مخصص بيطلب من المساعد يرد بقصيدة Haiku أصلية بناءً على السياق المستَرجع المتعلق بالشعر التحفيزي.

## مركبات الاستجابة (Response Synthesizers)

مستندات Llama Index عندها تفاصيل كتير عن كل مركب استجابة. 

## أوضاع الاستجابة (Response Modes)

في LlamaIndex، أوضاع الاستجابة بتُستخدم لتحديد إزاي النظام لازم يعالج ويرجع نتائج الاستعلام. كل وضع استجابة مصمم للتعامل مع أنواع مختلفة من الاستعلامات وحالات الاستخدام، بيوفر مرونة وتخصيص في إزاي تتفاعل مع بياناتك.

### ⚗️ Refine (التحسين)

Refine هي طريقة تكرارية لتوليد استجابة.

في البداية، بنستخدم السياق في العقدة الأولى والاستعلام لإنشاء إجابة أساسية. بعدين، بنحسن الإجابة دي عن طريق إدخالها، مع الاستعلام وسياق العقدة التانية، في "prompt التحسين" لتوليد إجابة محسّنة.

عملية التحسين دي بتستمر عبر N-1 عقدة، مع N هو إجمالي عدد العقد. بيعمل استدعاء منفصل لـ LLM لكل عقدة/قطعة مستَرجعة. الوضع ده كويس لتوليد إجابات أكثر تفصيلاً.

### 🤏 Compact (المدمج)

وضع Compact and refine بيدمج الأول قطع النص في قطع موحدة أكبر بتستخدم نافذة السياق المتاحة بشكل أكمل، بعدين بيحسن الإجابات عبرهم. الوضع ده أسرع من refine لأننا بنعمل استدعاءات أقل لـ LLM.

الوضع ده مفيد لما تعايز تقلل عدد استدعاءات LLM مع الاستمرار في تحسين الإجابة.

### 📝 Simple Summarize (التلخيص البسيط)

دمج كل قطع النص في واحدة وعمل استدعاء لنموذج اللغة الكبيرة. الاستدعاء هيفشل لو قطعة النص المدمجة تتعدى حجم نافذة السياق.

كويس لأغراض التلخيص السريع، بس ممكن يفقد التفاصيل بسبب الاقتطاع.

### 🌴 Tree Summarize (التلخيص الشجري)

بناء فهرس شجري للعقد المرشحة بطريقة من أسفل لأعلى بعدين استخدام prompt تلخيص بناءً على الاستعلام. إرجاع العقدة الجذر كاستجابة نهائية. لما يتعيّن الوضع ده، النظام بيتوجه له إنه يكرر على كتير، إن ما كانش كل، المستندات عشان يركب إجابة، واللي ممكن يؤدي لنتائج تلخيص أحسن.

الوضع ده مفيد بشكل خاص لاستعلامات التلخيص، حيث الهدف هو توفير ملخص شامل لمجموعة من النصوص أو موضوع محدد.

### 🤖 Generation (التوليد)

تجاهل السياق، بس استخدم LLM لتوليد استجابة.

### ❌ No Text (بدون نص)

الوضع ده بيشغل المستَرجع بس لجلب العقد اللي كانت هتتبعت لـ LLM، بدون تركيب استجابة نهائية فعلياً. العقد بعدين ممكن تفحصها بالتحقق من `response.source_nodes`.

### 📏 Accumulate (التراكم)

الوضع ده بيطبق الاستعلام على كل قطعة نص مع تراكم الاستجابات في مصفوفة. بيرجع سلسلة مدمجة من كل الاستجابات.

الوضع ده كويس لما تحتاج تشغل نفس الاستعلام بشكل منفصل ضد كل قطعة نص.

### Compact Accumulate (التراكم المدمج)

في وضع compact and accumulate، قطع النص بتتدمج في قطع أكبر لاستخدام نافذة السياق بشكل أحسن. الإجابات بعدين بتتراكم لكل قطعة وبترجع كدمج. الوضع ده أسرع من accumulate لأنه بيقلل الاستدعاءات لـ LLM.

</div>
