# سير عمل LlamaIndex متقدم - مستَرجعات متعددة + Streaming

<div dir="rtl">

## نظرة عامة

الـ notebook ده بيوضح إزاي تبني **سير عمل RAG بمستَرجعات متعددة** باستخدام واجهة برمجة `Workflows` الجديدة في LlamaIndex.

## ✅ المميزات:

- إدخال فهارس متعددة
- استرجاع متوازي
- مخرجات LLM بنظام streaming

## التنصيبات المطلوبة

لازم تنصب الحزم دي (تشغل مرة واحدة):
- `llama-index-core`
- `llama-index-llms-openai`
- `llama-index-embeddings-huggingface`
- `llama-index-llms-ollama`
- `llama-index-embeddings-cohere`

## المفاهيم الأساسية

### 🧩 حدث مخصص لنتائج الاسترجاع المتعددة

بنعرّف حدث خاص `MultiRetrieveEvent` لحمل نتائج الاسترجاع من فهارس متعددة.

الحدث ده بيحتوي على dictionary فيه:
- المفتاح: اسم الفهرس
- القيمة: قائمة العقد المستَرجعة

### 🧠 تعريف سير عمل RAG متعدد المستَرجعات

الـ `MultiRAGWorkflow` هو سير عمل التوليد المعزز بالاسترجاع بمستَرجعات متعددة ومخرجات streaming.

## خطوات سير العمل

### الخطوة 1: الإدخال (Ingest)

**الوظيفة:** `ingest`

**الإجراءات:**
- تحميل المستندات من مجلدات متعددة
- بناء فهارس متجهات لكل مجموعة مستندات
- استخدام نموذج التضمين المحدد
- تخزين الفهارس في السياق

**المخرج:** رسالة نجاح بعدد الفهارس المبنية

### الخطوة 2: الاسترجاع (Retrieve)

**الوظيفة:** `retrieve`

**الإجراءات:**
- استرجاع من كل الفهارس للاستعلام المعطى
- تشغيل الاسترجاع بشكل متوازي
- جمع النتائج من كل مصدر
- عرض عدد العقد المستَرجعة من كل فهرس

**المخرج:** `MultiRetrieveEvent` يحتوي على كل النتائج

### الخطوة 3: توليد الإجابة (Generate Answer)

**الوظيفة:** `generate_answer`

**الإجراءات:**
- دمج العقد المستَرجعة من كل المصادر
- إنشاء prompt شامل بالسياق المدمج
- استخدام LLM مع streaming
- عرض التوكنات أثناء التوليد

**المخرج:** إشعار باكتمال الstreaming

## تشغيل السير العمل

### مثال على الاستخدام:

الكود بيشغل السير العمل على مرحلتين:

**المرحلة 1:** الإدخال
- تحميل المستندات من مجلدات البيانات المحددة
- بناء الفهارس

**المرحلة 2:** الاستعلام
- تشغيل استعلام ضد كل الفهارس
- استرجاع النتائج ذات الصلة
- توليد إجابة شاملة مع streaming

## الفوائد الرئيسية

### 📚 مصادر متعددة

ممكن تستعلم عن فهارس متعددة في نفس الوقت، بيجمع معلومات من مصادر بيانات مختلفة.

### ⚡ استرجاع متوازي

الاسترجاع من كل الفهارس بيحصل بشكل متوازي، بيحسن الأداء.

### 📡 Streaming Output

مخرجات LLM بتتعرض بشكل تدريجي (streaming)، بيحسن تجربة المستخدم.

### 🔧 قابلية التوسع

سهل تضيف مصادر بيانات جديدة بإضافة مجلدات أكتر للمعالجة.

## ملاحظات مهمة

- السير العمل بيدير الأحداث تلقائياً بين الخطوات
- كل خطوة معرّفة بوضوح بمدخلاتها ومخرجاتها
- النظام بيدعم timeout وverbose logging
- ممكن تخصص نماذج LLM والتضمين حسب احتياجاتك

</div>
