# إزاي تنشئ مجموعة تقييم

<div dir="rtl">

## ملحوظة عن التنصيبات

لازم تنصب الحزم دي على البيئة بتاعتك:

- `pip install datasets`
- `pip install llama-index-embeddings-fastembed`
- `pip install llama-index-llms-mistralai`

## استخدام نماذج اللغة الكبيرة

أنا باستخدم OpenAI هنا لأن Cohere عندها حدود للمعدل للطبقة المجانية. مش محتاج تشغل الكود ده بنفسك لو مش عايز تتحمل تكاليف من OpenAI. هرفع مجموعة البيانات على Hugging Face Hub وهوريك إزاي تحملها من هناك لما نحتاجها.

## إعداد المستندات للتقييم

احنا خلصنا تنظيف البيانات بتاعتنا قبل كده. تفتكر إننا خزنا كائنات `Document` على القرص باستخدام Docstore بطريقة إن كل كائن Document بيمثل نص منظف من صفحة كتاب.

## إنشاء مجموعة من `Documents` لمجموعة التقييم

### 📚 `group_documents_by_author`

دالة مساعدة بترتب مجموعة من المستندات في مجموعات بناءً على مين كتبها.

**🗂️ إزاي بتشتغل:**

بتنشئ قاموس حيث اسم كل مؤلف مرتبط بكل المستندات اللي كتبها:

- بتبدأ بقاموس فاضي جاهز يتملى بأزواج المؤلف-المستند
- بتمر على كل مستند، بتتحقق من اسم المؤلف وبتضيف المستند تحت المؤلف المناسب في القاموس
- لو مستند مابيذكرش مؤلف، بتتخطى إضافة المستند ده مع ملاحظة تحذير

**📝 المدخل:** بياخد قائمة من كائنات `Document`، كل واحد فيه بيانات وصفية بتتضمن حقل `author` (اسم مؤلفه).

**🔖 المخرج:** بيخرج قاموس بيجمع كل المستندات حسب مؤلفيها المعنيين.

### 📚 `sample_documents`

بتختار عدد محدد من المستندات عشوائياً من مجموعة كل مؤلف ضمن قاموس مجمع.

**🎲 منطق الاختيار:**

بتحاول تجيب عدد محدد من المستندات لكل مؤلف. لو مؤلف ماعندوش مستندات كفاية، بتنبهك:

- بتبدأ بقائمة فاضية لتخزين العينات المختارة
- بتكرر على كل مؤلف، بتاخد في الاعتبار المستندات اللي فيها أكتر من 500 حرف بس، بتتحقق لو في مستندات كفاية لتلبية متطلبات الاختيار
- بتختار عشوائياً العدد المطلوب من المستندات من المتاحة، بتضيفهم لقائمة العينات الكلية
- بتصدر تحذير لو المستندات تحت مؤلف قليلة جداً لتلبية رقم الاختيار

**📝 المدخل:** بيستقبل قاموس حيث المؤلفين هم المفاتيح والقيم هي قوائم مستنداتهم، مع عدد اختياري من المستندات للاختيار لكل مؤلف.

**🔖 المخرج:** بيخرج قائمة من المستندات المختارة عشوائياً من كل المؤلفين، ملتزم بالعدد المحدد لكل مؤلف لما ممكن.

## فحص الصحة

بعد اختيار المستندات، بنعمل فحص صحة للتأكد من إن العينات موزعة بشكل صحيح عبر المؤلفين المختلفين.

## إنشاء مجموعة التقييم باستخدام prompts مخصصة

### 🤖 + ❓ توليد أسئلة من السياق

هنستخدم GPT-3.5-Turbo لتوليد أسئلة من `Documents` بتاعتنا.

**إيه اللي الدالة بتعمله:**

- تهيئة قاموس فاضي `results` لتخزين الاستجابات والسياقات
- التكرار على كل مستند `doc` في `docs_for_eval_set`
- لكل مستند، بنولد الـ prompt باستخدام `QUESTION_GEN_PROMPT_TEMPLATE` ومحتوى المستند
- نجيب الاستجابة من الـ LLM باستخدام `question_gen_llm.complete(prompt)`
- نخزن الاستجابة كمفتاح، ومحتوى المستند كقيمة بالمفتاح "context" في قاموس النتائج

### 🤖 + 💬 إنشاء إجابات باستخدام السؤال المولد والسياق

باستخدام GPT-3.5-Turbo (للحفاظ على التكاليف منخفضة، طبعاً تقدر تستخدم GPT-4-Turbo)، هنولد إجابات باستخدام الأسئلة اللي أنشأناها للتو بالإضافة للسياق.

## 🧐 قد إيه أسئلتنا كويسة؟

ممكن طبعاً تعمل الجزء ده قبل توليد الإجابات، لو عايز... بس هنعمله دلوقتي.

هنا هنستخدم GPT-4-Turbo للحكم على قد إيه السؤال كويس بناءً على السياق. هنكتب prompt بيعمل ده ونقيم كل سؤال على مقياس من 1-5.

## حفظ مجموعة التقييم

بعد إنشاء الأسئلة والإجابات وتقييمها، بنحفظ مجموعة التقييم على Hugging Face Hub.

## تقدر تلاقي مجموعة البيانات على Hugging Face

مش لازم تشغل الأمثلة هنا لو مش عايز تتحمل تكاليف من OpenAI.

[هنا مجموعة البيانات](https://huggingface.co/datasets/harpreetsahota/LI_Learning_RAG_Eval_Set). تقدر تدوس وتستكشف باستخدام عارض مجموعات البيانات. لو سجلت حساب على Hugging Face، اتفضل [تابعني](https://huggingface.co/harpreetsahota)!

</div>
