# RAG الوكيلي باستخدام وكلاء البيانات

<div dir="rtl">

## 🧠 المفهوم

وكلاء البيانات هم عمال معرفة مدعومين بـ LLM في LlamaIndex يقدروا بذكاء يؤدوا مهام مختلفة فوق بياناتك، في كل من وظيفة "القراءة" و"الكتابة".

### قدراتهم:

- 🔍 تنفيذ البحث والاسترجاع الآلي عبر أنواع مختلفة من البيانات - غير مهيكلة، شبه مهيكلة، ومهيكلة.

- 📞 استدعاء أي واجهة برمجة خدمة خارجية بشكل منظم، ومعالجة الاستجابة + تخزينها لاحقاً.

في الإطار ده، الوكلاء خطوة أبعد من محركات الاستعلام بتاعتنا في إنهم مش بس يقدروا "يقروا" من مصدر بيانات ثابت، لكن يقدروا بشكل ديناميكي يدخلوا ويعدلوا البيانات من مجموعة متنوعة من الأدوات المختلفة.

## 🛠️ بناء وكيل بيانات

بناء وكيل بيانات بيتطلب المكونات الأساسية التالية:

### 1. 🌀 حلقة استدلال

الوكيل بيحتاج حلقة استدلال لاتخاذ قرارات بناءً على المدخل.

### 2. 🧰 تجريدات الأدوات

واجهات برمجية (APIs) أو أدوات للتفاعل معها.

وكيل البيانات بيتهيأ بمجموعة من واجهات برمجة التطبيقات، أو الأدوات، للتفاعل معها؛ واجهات برمجة التطبيقات دي ممكن الوكيل يستدعيها لإرجاع معلومات أو تعديل الحالة. بناءً على مهمة مدخل، وكيل البيانات بيستخدم حلقة استدلال ليقرر أي أدوات يستخدم، في أي تسلسل، والمعاملات لاستدعاء كل أداة.

## إضافة بيانات وصفية للعقد يدوياً

ممكن نضيف بيانات وصفية مخصصة للمستندات لتحسين عملية الاسترجاع. مثلاً، نضيف معلومات عن كل مفكر وإيه اللي مشهور بيه.

### مثال للبيانات الوصفية:

- **Naval Ravikant**: معروف برؤاه عن إزاي تبني الثروة وتحقق السعادة من خلال تطوير معرفة محددة، احتضان المساءلة، لعب ألعاب طويلة الأجل، وفهم قوة الفائدة المركبة في كل مجالات الحياة.

- **Balaji Srinivasan**: عنده رؤى عن إزاي تفكر بشكل مستقل، تحدد الفرص، وتبني مستقبل أفضل من خلال التطبيق الاستراتيجي للتكنولوجيا والاستدلال الواضح.

- **Paul Graham**: بيقدم نصائح عن عقلية الهاكرز، بيجادل إن الهاكرز حقيقةً صناع ومبدعين - شبيهين بالرسامين - يقدروا يستفيدوا من طريقة تفكيرهم الفريدة لدفع الحدود، تحدي الوضع الراهن، وتشكيل المستقبل من خلال التكنولوجيا وريادة الأعمال.

- **Nassim Nicholas Taleb**: بيجادل لـ 'Skin in the Game'، يعني إن وجود مصلحة شخصية في النتيجة ضروري للعدالة لأنه بيوائم الحوافز ويعرض الأفراد لكل من المكافآت المحتملة والمخاطر لقراراتهم.

- **Seneca**: بيقدم نصائح خالدة عن إزاي تزرع الحكمة، تبني مرونة عقلية، وتعيش حياة هدف ورضا بالتركيز على الأساسي، إتقان العواطف، والمواءمة مع الطبيعة.

- **Bruce Lee**: بيقدم حكمة عميقة عن تحسين الذات، النمو الشخصي، وفلسفة الفنون القتالية، بيؤكد على أهمية القدرة على التكيف، التعبير عن الذات، واحتضان مسارك الفريد في الحياة.

## إضافة بيانات وصفية تلقائياً

بالإضافة للبيانات الوصفية اليدوية، ممكن نستخرج بيانات وصفية تلقائياً باستخدام:

### المستخرجات:

- **QuestionsAnsweredExtractor**: بيستخرج أسئلة ممكن قطعة النص تجاوب عليها
- **KeywordExtractor**: بيستخرج الكلمات المفتاحية من النص

## إنشاء VectorStoreInfo

`VectorStoreInfo` مكون بيُستخدم في تكوين مخازن المتجهات داخل إطار عمل LlamaIndex. بيوفر بيانات وصفية عن المحتوى والهيكل للبيانات المخزنة في مخزن المتجهات. البيانات الوصفية دي بتساعد في عمليات الفهرسة والاستعلام، بتضمن إن مخزن المتجهات محسّن لأنواع محددة من الاستعلامات وفئات البيانات.

### خطوات استخدام VectorStoreInfo:

1. **تعريف معلومات البيانات الوصفية**: حدد تفاصيل عن فئات البيانات والسمات ذات الصلة التانية.

## التكامل مع Vector Index AutoRetriever

بعد كده، هننشئ كائن Pydantic الأساسي لضمان التوافق مع طبقة التطبيق. الكائن ده بيتحقق من إن الاستجابة من نقطة نهاية OpenAI بتطابق المخطط المحدد.

بعد تعريف `VectorStoreInfo`، تقدر تستخدمه لتكوين `VectorIndexAutoRetriever`. المستَرجع ده بيضبط إعدادات الاستعلام تلقائياً بناءً على البيانات الوصفية المقدمة.

## أداة Auto Retriever الوظيفية

الأداة دي هتستفيد من نقطة نهاية OpenAI الوظيفية لاختيار فلتر البيانات الوصفية الصحيح والاستعلام عن الفهرس المفلتر - بتبص بس على العقد بالبيانات الوصفية المطلوبة.

## إزاي الوكيل بيشتغل؟

استعلام المستخدم بيتعالج من خلال محرك استعلام، بيتحول، وبعدين بيتفلتر ويُسترجع من قاعدة بيانات متجهات لتوفير استجابة ذات صلة.

### رسم تخطيطي مبسط:

<img src="https://i.imgur.com/AICDPav.png" width="50%">

[المصدر: AI Makerspace](https://www.youtube.com/watch?v=wYZJq8CNmTw&t=3s)

### الخطوات:

#### 1. استعلام المستخدم

المستخدم بيدخل استعلام.

#### 2. محرك الاستعلام

- الاستعلام بيتبعت الأول لتطبيق `QueryEngineRetriever`، واللي جزء من نظام محرك الاستعلام.
- الـ `QueryEngineRetriever` بعدين بيتفاعل مع تطبيق `VectorStoreData` لمعالجة الاستعلام أكتر.

#### 3. OpenAI API

الـ `VectorStoreData` بيتواصل مع نموذجين من OpenAI API:
- **نموذج اللغة**: بيفهم ويولد استجابات متماسكة بناءً على الاستعلام.
- **نموذج التضمين**: بيحول الاستعلام والبيانات لتمثيلات متجهة لسهولة المعالجة والاسترجاع.

#### 4. مخزن المتجهات

- المعلومات المعالجة بتتبعت لـ `Vector Database` داخل نظام Vector Store.
- الـ `Vector Database` بيخزن البيانات المتجهة ويقدر يفلترها بناءً على البيانات الوصفية من خلال تطبيق `Metadata Filter`.
- عملية التصفية دي بتنتج `Filtered List of Nodes`، واللي بتمثل أكثر قطع المعلومات صلة بالاستعلام.

#### 5. تجميع النتيجة

- المعلومات المفلترة بترجع لتطبيق `VectorStoreData`، واللي بيجمع الاستجابة النهائية.
- الاستجابة المجمعة دي بعدين بتتبعت لتطبيق `QueryEngineRetriever`.

#### 6. عرض الاستجابة

أخيراً، المعلومات المعالجة وذات الصلة بتتعرض للمستخدم.

## واجهة برمجة تطبيقات منخفضة المستوى

<img src="https://docs.llamaindex.ai/en/stable/_static/agents/agent_step_execute.png" width="70%">

"الوكلاء" في LlamaIndex مكونة من كائنات `AgentRunner` بتتفاعل مع `AgentWorkers`:

### المكونات:

#### `AgentRunner`

المنظمين اللي بيديروا الحالة (بما فيها الذاكرة الحوارية)، بينشئوا ويحافظوا على المهام، بينفذوا الخطوات ضمن كل مهمة، وبيوفروا الواجهة عالية المستوى للتفاعل مع المستخدم.

#### `AgentWorker`

بيتحكموا في التنفيذ التدريجي للمهمة. بناءً على خطوة مدخل، `AgentWorker` مسؤول عن توليد الخطوة التالية. ممكن يتهيأوا بمعاملات ويتصرفوا على حالة متمررة من كائنات `Task` أو `TaskStep` بس ماعندهمش حالة مخزنة بطبيعتهم. الـ `AgentRunner` الخارجي بيستدعي `AgentWorker` ويجمع/يجمّع النتائج.

</div>
