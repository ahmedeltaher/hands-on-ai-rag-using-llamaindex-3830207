# محرك الاستعلام الجماعي (Ensemble Query Engine)

<div dir="rtl">

## المفهوم

لما بتبني تطبيق توليد معزز بالاسترجاع (RAG)، التجربة مع خطوط أنابيب استعلام مختلفة - زي استرجاع top-k، البحث بالكلمات المفتاحية، والرسوم البيانية للمعرفة - غالباً ضرورية.

**الفكرة**: لو نقدر نستخدم استراتيجيات متعددة في نفس الوقت ويكون عندنا نموذج لغة (LLM):

### الوظائف:

1. يقيم صلة كل نتيجة استعلام
2. يركب النتائج في إجابة متماسكة

محرك الاستعلام الجماعي بيسمح لك تعمل ده بالضبط. الدليل ده بيشرح إزاي تجرب خطوط أنابيب استعلام واستراتيجيات مختلفة، تخلي LLM يقيم صلة كل نتيجة، ويركب الاستجابة النهائية.

## الأغراض الرئيسية

### 1. 🔄 استرجاع متعدد الاستراتيجيات

جرب استراتيجيات استرجاع متعددة مرة واحدة، زي استرجاع top-k، البحث بالكلمات المفتاحية، والرسوم البيانية للمعرفة. ده بيساعد على مقارنة فعالية مناهج مختلفة.

### 2. 🎯 تقييم الصلة

خلي LLM يقيم مدى صلة كل نتيجة مستَرجعة بالاستعلام الأصلي، بيضمن الأخذ في الاعتبار بس للمعلومات الأكثر صلة.

### 3. 💡 تركيب النتائج

خلي LLM يدمج أكثر المعلومات صلة من طرق استرجاع مختلفة، بيستفيد من فهمه اللغوي لإنشاء إجابة نهائية شاملة.

## إزاي تستخدم محرك الاستعلام الجماعي

### الخطوات:

#### 1. إعداد أدوات الاسترجاع

تكوين أدوات استرجاع مختلفة، زي أداة بحث بالكلمات المفتاحية وأداة بحث متجه.

#### 2. تكوين محرك استعلام الموجه (Router Query Engine)

إعداد محرك استعلام موجه مع محدد لاختيار الاستِرجاعات ذات الصلة وملخص لتركيب الإجابة النهائية.

#### 3. تشغيل الاستعلامات

استخدام محرك استعلام الموجه لمعالجة الاستعلامات وإرجاع استجابات مركبة بتستفيد من استراتيجيات استرجاع متعددة مقيّمة بواسطة LLM.

## فهرس جدول الكلمات المفتاحية البسيط

### `SimpleKeywordTableIndex`

الكلاس `SimpleKeywordTableIndex` نسخة مبسطة من نظام فهرسة مبني على الكلمات المفتاحية.

#### بناء الفهرس:

خلال بناء الفهرس، الـ KeywordTableIndex بياخد مجموعة بيانات من مستندات نصية، بيقطعها، وبيستخدم GPT لاستخراج كلمات مفتاحية ذات صلة. الكلمات المفتاحية دي بتتخزن في جدول بيرجع لقطعة النص المعنية.

**الخطوات:**
- تقسيم المستندات النصية لقطع
- استخراج كلمات مفتاحية لكل قطعة
- تخزين الكلمات المفتاحية في جدول بيرجع لقطع النص

#### أوضاع الاستعلام:

**الافتراضي (Default):** بيستخدم GPT لاستخراج الكلمات المفتاحية وبيبني إجابات عن طريق تحسين قطع النص.

**البسيط (Simple):** بيستخدم regex لاستخراج الكلمات المفتاحية (منفذ بواسطة `SimpleKeywordTableIndex`).

خلال الاستعلام، الـ KeywordTableIndex بيستخرج كلمات مفتاحية ذات صلة ويستخدمها لاسترجاع مجموعة من معرفات قطع النص المرشحة. الإجابة الأولية بتتبنى باستخدام قطعة النص الأولى وبعدين بتتحسن مع القطع اللاحقة.

## QueryEngineTool (أداة محرك الاستعلام)

الأدوات هي تجريدات مصممة لاستخدامها بواسطة وكلاء البيانات أو نماذج LLM وبتوفر طريقة منظمة لهم لأداء المهام.

`QueryEngineTool` نوع محدد من الأداة مصمم للتواجه مع ولف محركات الاستعلام الموجودة. بيمكّن الوكلاء من تنفيذ استعلامات معقدة عن طريق الاستفادة من قدرات محرك الاستعلام الأساسي.

### حالات الاستخدام:

- **تكامل محركات الاستعلام**: بيسمح للوكلاء يتفاعلوا مع محركات الاستعلام ووكلاء تانيين.
- **التعامل مع الاستعلامات المعقدة**: بيساعد على تنفيذ استعلامات متطورة وعمليات استرجاع بيانات.

## تعريف محرك استعلام موجه (Router Query Engine)

الـ `LLMMultiSelector` بيستخدم نماذج LLM لاتخاذ القرارات. بيستخدم prompt لعرض الخيارات على LLM، واللي بعدين بيختار الخيارات الأكثر صلة بناءً على الاستعلام. ممكن يُستخدم لوحده أو متكامل في محركات الاستعلام والمستَرجعات.

### الوظائف الرئيسية:

#### 1. اختيار مصدر البيانات

بيختار أفضل مصدر بيانات من خيارات متعددة.

#### 2. القرارات التشغيلية

بيقرر هل يؤدي تلخيص أو بحث دلالي.

#### 3. التوجيه المتعدد

بيقيم خيارات متعددة في نفس الوقت ويدمج النتائج.

### حالات الاستخدام:

- اختيار مصدر البيانات الصحيح
- الاختيار بين التلخيص والبحث الدلالي
- دمج النتائج من خيارات متعددة

## الفوائد

محرك الاستعلام الجماعي بيمكنك تستغل نقاط القوة لطرق استرجاع مختلفة وقدرات الاستدلال لنماذج LLM في واجهة استعلام موحدة. بيبسط التجربة مع مناهج مختلفة، بيساعدك تلاقي التكوين الأمثل لتطبيق RAG بتاعك.

</div>
